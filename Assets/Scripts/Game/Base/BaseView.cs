using UnityEngine;

namespace Game
{
    /// <summary>
    /// 基地场景对象标记组件。
    /// 用于点击选择、显示目标信息等表现层逻辑。
    /// 基地生命等核心数据仍然由 BaseManager 管理。
    /// </summary>
    public sealed class BaseView : MonoBehaviour
    {
    }
}
